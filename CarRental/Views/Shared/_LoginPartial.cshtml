@using Microsoft.AspNetCore.Identity
@using CarRental.DataBase.Models

@inject SignInManager<Client> SignInManager
@inject UserManager<Client> UserManager


@if (SignInManager.IsSignedIn(User))
{
    <ul class="navbar-nav ml-auto">
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Logout">Wyloguj</a>
            @*@Html.ActionLink("Wyloguj", "Logout","Home", new { @class = "nav-link text-dark" })*@
           @* <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="nav-link btn btn-link text-dark">Wyloguj się</button>
            </form>*@
        </li>
        
        
            <li class="nav-item">
            <a id="manage" class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index">@UserManager.GetUserName(User)!</a>
            </li>
        </ul>
}
else
{
    <ul class="navbar-nav">

    <li class="nav-item">
        <a class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
    </ul>

}
